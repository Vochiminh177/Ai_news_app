const Heading = ({ title }) => {
  return (
    <div className="w-full px-4 py-2 text-white font-semibold bg-gradient-to-r from-blue-500 to-white">
      {title}
    </div>
  );
};

export default Heading;
